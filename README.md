You can get quite far with a single spec and a single sentence prompt.

Small SPA to help calculate the required heat output of radiators for a room. Generated entirely from:

>Build a fully deployed SPA to GitHub Pages that will allow me to calculate the heat output, in watts and BTU, required for radiators in a room, so that I am able to correctly size radiators when doing any home renovations
